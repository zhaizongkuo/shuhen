# 给 AI 的指路

这是**书痕的代码仓库**，公开。写代码、修 bug、发版本在这里。

## 🔴 判断依据不在这个仓库里

产品方向、竞品数据、上架经验、以及一套 18 条判据，都在
**`D:\pathfinder`**（私有）。那边是调研仓库，本仓库是它孵化出的产品。

**Claude Code 的会话按目录隔离**，在这里开窗口默认读不到那边的东西。
遇到下列问题时，用绝对路径去读，不要凭空推断：

| 问题 | 读哪里 |
| --- | --- |
| 这个功能该不该做 / 下一步做什么 | `D:\pathfinder\STATUS.md` |
| 为什么当初这么定（市场、渠道、命名、变现） | `D:\pathfinder\CRITERIA.md` |
| 上架表单怎么填、踩过什么坑 | `D:\pathfinder\launch\SUBMIT.md` |
| 冷启动怎么做、什么时候该停 | `D:\pathfinder\launch\LAUNCH.md` |
| 商店文案与截图 | `D:\pathfinder\launch\` |

**分工**：写代码在这边，做判断去那边。

## 本仓库的几条硬约束

**1. 不许出现网络请求。** 隐私政策与 README 都写着「代码里没有任何一处
网络请求」，`checkprivacy.mjs` 在构建时强制校验。要加任何联网功能，
**必须同时改隐私政策**，两者不能不一致 —— 微软明确把不一致视为违规。

**2. 改了文案要看长度。** 商店对名称/简述有字符上限，
`checklimits.mjs` 会在构建时拦下，别等提交商店才发现。

**3. 锚定算法（`src/core/anchor.js`）改动必须先跑测试。**
里面带 🔴 标记的每条测试都对应一个真实踩过的坑，
尤其是「位置权重不能推翻上下文」那条不变量。

**4. 发布流程**：`npm test` → `npm run build` → `pack.ps1` → Partner Center。
版本号在 `src/manifest.json`，每次提交商店都必须递增。
